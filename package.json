{
  "name": "r2drive",
  "version": "0.1.0",
  "private": true,
  "description": "Private personal drive powered by Cloudflare R2 and a single Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev --config worker/wrangler.toml",
    "deploy": "wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.11.1"
  }
}
